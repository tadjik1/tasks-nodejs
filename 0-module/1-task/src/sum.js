/**
 * Функция, складывающая два числа
 * @param {number} a - первый параметр
 * @param {number} b - второй параметр
 * @throws {TypeError} - Бросает ошибку если один из параметров не число
 * @returns {number} - сумма параметров
 */

function sum(a, b) {
  /* ваш код */
}

module.exports = sum;
